#pragma once

#include <cstdint>
#include <vector>

using u8 = uint8_t;
using i32 = int32_t;
using usize = std::size_t;

namespace rsh {

using Bytes = std::vector<u8>;
using Code = std::vector<u8>;

}; // namespace rsh
