// Entry point for RCP, which compiles with -I<rsh>/src/bc2c and expects the
// full bytecode-JIT internals under this name. Distinct from the shared
// common2c/runtime_internals.h, hence #pragma once rather than a guard macro.
#pragma once
#include "opcodes_internals.h"
