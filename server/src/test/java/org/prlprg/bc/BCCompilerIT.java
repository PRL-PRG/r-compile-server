package org.prlprg.bc;

import org.prlprg.sexp.SEXP;

public class BCCompilerIT extends AbstractBCCompilerTest implements StdlibClosureTests {

  @Override
  public SEXP eval(String source) {
    return super.eval(source);
  }
}
