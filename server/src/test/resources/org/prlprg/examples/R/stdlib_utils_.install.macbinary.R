#? stdlib
`.install.macbinary` <- function (pkgs, lib, repos = getOption("repos"), contriburl = contrib.url(repos, 
    type = "mac.binary"), method, available = NULL, destdir = NULL, 
    dependencies = FALSE, lock = getOption("install.lock", FALSE), 
    quiet = FALSE, ...) 
{
}
