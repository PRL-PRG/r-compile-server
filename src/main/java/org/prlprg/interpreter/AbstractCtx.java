package org.prlprg.interpreter;

/** Context type of abstract or concrete interpretation (different kinds of interpretation use different subtypes).
 * The {@code C} parameter in {@link Interpreter} and {@link Stepper}. */
public interface AbstractCtx<S extends AbstractSEXP> {
}
