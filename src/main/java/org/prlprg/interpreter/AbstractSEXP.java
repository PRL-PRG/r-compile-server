package org.prlprg.interpreter;

/** Value type of abstract or concrete interpretation (different kinds of interpretation use different subtypes).
 * The {@code S} parameter in {@link Interpreter}, {@link Stepper}, and {@link AbstractCtx}. */
public interface AbstractSEXP {
}
