package org.prlprg.optimizations;

import org.prlprg.ir.cfg.CFG;

/** Remove instructions that are unused. */
class DeadCodeElimination implements OptimizationPass {
  @Override
  public void doApply(CFG cfg, OptimizationContext ctx) {
    // TODO
  }
}
